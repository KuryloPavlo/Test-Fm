import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        marginLeft: 10
    },
    textName: {
        fontSize: 22,
    },
    textOpacity: {
        opacity: 0.5,
        marginVertical: 10,
    }
})

export default styles;