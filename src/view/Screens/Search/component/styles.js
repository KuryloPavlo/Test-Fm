import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        borderWidth: 0.5,
        marginVertical: 5,
        marginHorizontal: 5,
        paddingLeft: 20,
        borderRadius: 5,
    }
})

export default styles;