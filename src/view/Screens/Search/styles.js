import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1, 
    },
    textInput: {
        borderWidth: 0.5,
        borderColor: 'gray',
    }
})

export default styles;